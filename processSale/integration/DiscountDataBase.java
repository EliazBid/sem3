package processSale.integration;

public class DiscountDataBase {

	/**
     	 * Unknow how this database operates thus no attributes are created. 
     	 */

    	/**
     	 * Creates an instance of the discount database.
     	 */
    	public DiscountDataBase() {
        
    	}

    	/**
     	 * Fetches the discount from the database.
     	 * Never actually used in seminar 3 but would be used in a real system.
     	 */
    	public void fetchDiscount() {
        
    	}

}
